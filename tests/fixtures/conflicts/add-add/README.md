# add-add — both branches added content where base was empty

Classic configuration-merge case: a previously-empty block (or a
freshly-created file) gets independent additions on both branches.
Expected: ADD_ADD.

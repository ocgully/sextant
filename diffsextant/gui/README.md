# gui/ — placeholder for phase 1C

The DiffSextant web UI (Preact + esm.sh + single-port stdlib server,
same stack as Hopewell / Pedia) lands here in phase 1C of HW-0056.
Until then, the CLI and JSON output are the complete product surface.

See `patterns/drafts/diffsextant-plan.md` §6 in AgentFactory for the
full design — operations view, classic-text view (with semantic
overlay), conflict resolver, timeline, and the AI-conversation hand-off
pattern.
